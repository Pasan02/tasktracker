package com.tracker.tasktracker.service;import java.util.Optional;import com.tracker.tasktracker.dto.UserDto;import com.tracker.tasktracker.model.User;public interface UserService {    User registerUser(String firstName, String lastName, String email, String password);        Optional<User> authenticateUser(String email, String password);        Optional<User> getUserById(Long id);        Optional<User> getUserByEmail(String email);        UserDto convertToDto(User user);        User updateUserProfile(Long userId, String firstName, String lastName, String avatar);        boolean updatePassword(Long userId, String currentPassword, String newPassword);        boolean existsByEmail(String email);        User saveUser(User user);}